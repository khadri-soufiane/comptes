@using Comptes_WebAPI.Models
@model EditAccountViewModel
@{
    ViewData["Title"] = "Password Recovery";
}

<div class="container col-6" style="margin-top:10px;">
    <div class="card border-secondary">
        <div class="card-header text-white bg-secondary">
            <strong>Password Recovery</strong>
        </div>

        <div class="card-body">
            <form asp-action="PasswordRecoveryPage" method="post">
                <div class="form-group">
                    <label asp-for="RecoveryCode"></label>
                    <input asp-for="RecoveryCode" required class="form-control" />
                </div>

                <div class="form-group">
                    <label asp-for="NewPassword"></label>
                    <input asp-for="NewPassword" required type="password" class="form-control" />
                </div>

                <div class="form-group">
                    <label asp-for="NewPasswordAgain"></label>
                    <input asp-for="NewPasswordAgain" required type="password" class="form-control" />
                </div>

                <div class="form-group">
                    <div required class="g-recaptcha" data-sitekey="6LfFtJoUAAAAAJr_Sa2aadi9-uzzBIK5RdlFCiNf"></div>
                    <span id='message-captcha'></span>
                </div>

                <input value="Reset" type="submit" class="btn btn-sm btn-outline-primary float-right" />
            </form>
        </div>
    </div>

    <script src='https://www.google.com/recaptcha/api.js' async defer type="text/javascript"></script>

    <script>
        $("form").submit(function (event) {

            var recaptcha = $("#g-recaptcha-response").val();
            if (recaptcha === "") {
                event.preventDefault();
                $('#message-captcha').html('Please check the Captcha').css('color', 'red');
            }
        });
    </script>
</div>

